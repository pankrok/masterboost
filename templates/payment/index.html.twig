{% extends 'base.html.twig' %}

{% block title %}{{ 'basket'|trans }}{% endblock %}

{% block body %}
<div class="col">
<div class="card m-1">
      <div class="card-body">
        {{ 'amount'|trans }}: {{ amount }} PLN
      </div>
    </div>
    <hr />
    {{ tpay|raw }}
</div>
{% endblock %}
