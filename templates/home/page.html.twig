{% extends 'base.html.twig' %}

{% block title %}{{ page.name|trans }}{% endblock %}


{% block body %}
<div class="col">   
<div class="card">
<div class="card-header">
    <h1>{{ page.prefix|raw }} {{ page.name|trans }}</h1>
</div>
  <div class="card-body">
    {{ page.content|raw }}
  </div>
</div>          
</div>
{% endblock %}
